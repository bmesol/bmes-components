{
  "sourceType": "unambiguous", // Specifies the source type to be unambiguous, allowing Babel to determine the source type automatically
  "presets": [
    [
      "@babel/preset-env", // Babel preset for transforming JavaScript for specific environments
      {
        "targets": {
          "chrome": 100,
          "safari": 15,
          "firefox": 91
        }
      }
    ],
    "@babel/preset-react" // Babel preset for transforming JSX and React specific syntax
  ],
  "plugins": [] // Additional Babel plugins can be added here, currently empty
}